To start, please run the `./setup.sh` which will install the necessary virtual environment at `./env`

Next, if on `mac`, run `source venv/bin/activate`

Or, on *windows* run `source venv/Scripts/activate`
